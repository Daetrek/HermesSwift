import Foundation

enum AppConstants {
    /// Public-safe default: users enter their own Hermes API server URL in Settings.
    static let defaultGatewayURL = ""
    static let keychainService = "dev.hermesagent.HermesSwift"
    static let tokenAccount = "apiToken"
}
